apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minesweeper-source-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3-csi  
  resources:
    requests:
      storage: 20Mi